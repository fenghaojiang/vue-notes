---
title: 面向对象的程序设计  
date: 2021-07-16
---  

数据属性包含一个数据值得位置。在这个位置可以读取和写入值。数据属性有4个描述其行为的特性。  

+ \[\[Configurable]]: 表示能否通过delete删除属性从而重新定义属性，能否修改属性的特性，或者能否把属性修改为访问器属性。像前面例子种那样直接在对象上定义的属性，它们的这个特性默认值为true

+ \[\[Enumerable]]: 表示能否通过for-in循环返回属性。像前面例子中那样直接在对象上定义的属性，它们的这个特性默认值为true

+ \[\[Writable]]: 表示能否修改属性的值。下像前面例子中那样直接在对象上定义的属性，它们的这个特性默认值为true。

+ \[\[Value]]: 包含这个属性的数据值。读取属性值得时候，从这个位置读；写入属性值得时候，把新值保存在这个位置。这个特性的默认值为undefined  


要修改属性默认的特性，要使用ECMAScript5的Object.defineProperty()方法。这个方法接收三个参数：属性所在的对象、属性的名字和一个描述符对象。其中，描述符(descriptor)对象的属性必须是上述四个的一个或者多个  

```js
var person = {};
Object.defineProperty(person, "name", {
    writable: false,
    value: "Niko",
});
alert(person.name); //Niko
person.name = "kenny"; //严格模式下会报错
alert(person.name); //Niko  
```  

类似的规则也适用于configurable  

```js
var person = {};
Object.defineProperty(person, "name", {
    configurable: false,
    value: "Niko",
});
alert(person.name); //Niko
delete person.name; //严格模式下会报错
alert(person.name); //Niko  
```

把configurable设置为false，表示不能从对象中删除属性。如果对这个属性调用delete，则在非严格模式下什么也不会发生，而在严格模式下会导致错误。  



